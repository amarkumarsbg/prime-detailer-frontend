# Copy to .env:  Copy-Item env.example .env  (PowerShell)
# SQLite (default dev — no Postgres/Docker needed):
DATABASE_URL=file:./dev.db
# With Docker Postgres instead: npm run db:up then use:
# DATABASE_URL=postgresql://prime:prime@localhost:5432/primedetailer

JWT_SECRET=prime-detailer-dev-jwt-secret
PORT=4000
FRONTEND_ORIGIN=http://localhost:3000

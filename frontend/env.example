# Copy to .env.local for local builds, or set in Vercel → Environment Variables.

# Production: your Render (or other) API origin — no trailing slash.
# NEXT_PUBLIC_API_URL=https://prime-detailers-api.onrender.com

# Local dev: omit to use Next.js rewrite → /backend-api → http://127.0.0.1:4000

/** Max workshop services on one quotation estimate. */
export const MAX_QUOTATION_SERVICES = 10;

/** Max quantity per counter sale part line on a quotation. */
export const MAX_QUOTATION_PART_QTY = 10;
